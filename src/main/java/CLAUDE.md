# Java Instructions

- All classes should have tests, and all methods should be tested (except for getters that don't have any special
  behaviour). The test file should be named `<ClassName>Test.java`.
- Wildcard imports should never be used.
